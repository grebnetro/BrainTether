/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // Disabling strict mode prevents double-render issues with drag-and-drop libs in dev
}

module.exports = nextConfig
